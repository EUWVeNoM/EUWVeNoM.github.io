---
title: Tools

exclude: ['/scripts', '/Gemfile', '/Makefile', '/README.md']

permalink: pretty

collections:
  wadcoms:
    output: true

defaults:
  - scope:
      path: '_hackmurerie'
    values:
      layout: bin
